## Plan: Add Help & Support modal with WhatsApp contact

**Single file change:** `src/components/user/profile-view.tsx`

### What's there now
"Help & Support" is a row in the `appRows` array with `comingSoon: true` (shows a "Coming soon" pill, fires a toast on tap). No real content exists. The file already has a framer-motion modal pattern used by the **Appearance** switcher and **Logout** confirmation — I'll mirror that exactly for consistency.

### Changes

1. **Remove the placeholder row from `appRows`** (the `appRows` array currently has Language + Help & Support). Render the Help & Support `SettingsRowButton` separately with a dedicated `onClick` — exactly like `Appearance` is already rendered separately from `appRows`. Remove `comingSoon: true` so it shows a chevron instead of the "Coming soon" pill.

2. **Add modal state + Escape handling**
   - `const [helpOpen, setHelpOpen] = useState(false);`
   - Add an Escape-key `useEffect` mirroring the existing `appearanceOpen` one (closes the help modal on Escape).

3. **Add the Help & Support modal** (new `<AnimatePresence>` block, copied from the Appearance modal structure):
   - Backdrop button (`bg-black/40`) + centered `motion.div` with `rounded-3xl bg-card p-5 shadow-xl ring-1 ring-foreground/5`.
   - Header: a `HelpCircle` icon in a colored circle, title "Help & Support".
   - **Static info** paragraph: *"Have a question about your orders, account, or payments? Our team is here to help you get the most out of TapShop."*
   - **WhatsApp number line**: shows "+8801881649665" (e.g. with a `MessageCircle`/`Phone` icon and label "WhatsApp").
   - **"Call on WhatsApp" button**: an `<a href="https://wa.me/8801881649665" target="_blank" rel="noopener noreferrer">` styled like the existing primary buttons (rounded, primary color), with the exact text "Call on WhatsApp". `wa.me` drops the `+` and uses the international number (8801881649665). This is the first external link in the codebase, but it's the standard, correct way to open WhatsApp.

4. **Imports**: add `MessageCircle` (and/or `Phone`) from `lucide-react` to the existing import block.

### WhatsApp link detail
`https://wa.me/8801881649665` opens a WhatsApp chat with the number. (WhatsApp doesn't expose a pure voice-call deep link via `wa.me`, so the button opens the contact/chat — standard behavior. I'll keep the label exactly "Call on WhatsApp" as requested.) I'll keep the number defined as a small const near the top of the file, alongside the existing inline static data (`accountRows`/`appRows`), so it stays self-contained.

### No other files change
No new routes, no constants-file edits, no new components — everything fits inside `profile-view.tsx` and reuses the established design tokens (`bg-card`, `text-primary`, `ring-foreground/5`, `bg-primary/10`, etc.).